# 需求文档



项目名称：项目验收工作量核验系统

需求功能点：

1、菜单1、验收材料基础库，初始化本行所有项目的验收材料，主要解析PDF或图片、excel版本的验收材料中的工作内容统计表的数据，并存储到mysql中。字段如下。

| 公司名称 | 项目名称 | 姓名 | 实际开始时间 | 实际结束时间 | 实际投入(人天) | 实施投入(标准人天) | 工作内容 |
| -------- | -------- | ---- | ------------ | ------------ | -------------- | ------------------ | -------- |
|          |          |      |              |              |                |                    |          |

这个模块，增加批量初始化功能，可以批量进行初始化，解析PDF或图片、excel版本，建议使用大模型多模态方式更加准确


2、菜单2，项目验收材料校验，用户上传PDF或excle版本的项目验收文件，需要解析PDF文档中的的数据提取暂存到mysql数据库中一个临时表中



3、校验本次上传项目的工作内容统计表的数据，校验规则：同一个人是否在同一时间段重复投入，展示如下：姓名、重复开始时间、重复结束时间、重复天数。重复发生所在项目字段。校验不通过，请重新提交材料



4、如何上传的项目验收材料校验存在重复，校验不通过，该项目验收工作内容统计表的数据不能入基础库。校验通过的可纳入基础库



5、增加系统管理模块、用户登陆模块，系统管理模块包括用户管理、机构管理、角色管理


6、更新需求：2、验收材料基础库模块，批量初始化界面增加，admin超级管理操作时：
机构/公司选择的下拉，数据为机构管理模块的数据，如北京宇信、高伟达。2、普通用户yxadmin登陆时,该下拉框默认被选择为北京宇信的机构，因为yxadmin的归属机构为北京宇信。机构/公司名称 对应 列表中的公司名称，将数据填充到该字段


7、更新以下需求
（1）验收材料基础库模块解析功能调整：解析分两部分A、文档中工作内容时间统计表的内容。B、文档中合作公司人员考勤登记表的内容（需要新增）。
解析成功后进行校对，校对规则：同一个项目名称、某人员的实际开始、实际结束时间区间的工作日内，人员考勤登记表是否存在对应日期的的签字，无对应日期的签字进行标红，提示无考勤。计算会在登记表中签字的天数与文档中工作内容时间统计表的标准人天是一致。一致为通过，不一致则不通过。校对一致后，人员考勤登记表的内容的数据保存到数据库，主要提取内容项目名称，姓名、打卡日期（年月日）保存。
（2）验收材料基础库模块的界面调整，列表中，点击姓名可以查看该员工在实际开始和实际结束期间的考勤登记情况列表，并汇总天数。列表中增加一个字段，是否考勤是否校对通过.

8、验收材料基础库模块中，针对文档中合作公司人员考勤登记表的内容的AI的解析规则的提示词，调整如下
                                        1. 提取字段：
                                           - 项目名称：
                                           - 姓名
                                           - 日期（格式：2024年X月X日，如2024年7月1、2024年7月2……一直到2024年7月31）
                                           - 上午：单元格是否有内容/签字 → 有/无
                                           - 下午：单元格是否有内容/签字 → 有/无
                                          2. 提取规则：
                                             - 项目名称：表格上方信息技术服务项目名称的内容
                                             - 只提取姓名列里的真实人员姓名，忽略无关文字
                                             - 日期范围：1日—31日，年月可提取表格上方的年和月，组合成日期年月日。
                                             - 时间列的上午/下午的单元格为空 → 填“无”
                                             - 时间列的上午/下午的单元格有任何文字/符号 → 填“有”
                                          3. 输出要求：
                                             - 只输出标准 JSON 数组，不要解释、不要多余文字、不要表格
                                             - 每条数据包含：项目名称、姓名、日期、上午、下午。
解析后数据存储到Mysql数据中，attendance_record调整如下：字段包括 ID、work_record_id，项目名称、姓名、日期、上午、下午.考勤记录核验规则：work_record_id对应的考勤记录的对应的工作日上午或者下午“有”可以算做有效的一天并汇总为实际考勤天数，判断实际考勤天数与标准人天是否一致