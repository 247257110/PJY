<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.SysOrgMapper">

    <select id="listAll" resultType="org.example.entity.SysOrg">
        SELECT * FROM sys_org
        <where>
            <if test="orgName != null and orgName != ''">AND org_name LIKE CONCAT('%',#{orgName},'%')</if>
        </where>
        ORDER BY sort, id
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_org (org_name, org_code, parent_id, sort, status, remark)
        VALUES (#{orgName}, #{orgCode}, #{parentId}, #{sort}, #{status}, #{remark})
    </insert>

    <update id="update">
        UPDATE sys_org
        <set>
            <if test="orgName != null">org_name = #{orgName},</if>
            <if test="orgCode != null">org_code = #{orgCode},</if>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="status != null">status = #{status},</if>
            <if test="remark != null">remark = #{remark},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">DELETE FROM sys_org WHERE id = #{id}</delete>

    <select id="findById" resultType="org.example.entity.SysOrg">
        SELECT * FROM sys_org WHERE id = #{id}
    </select>

</mapper>
