<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.SysRoleMapper">

    <select id="listAll" resultType="org.example.entity.SysRole">
        SELECT * FROM sys_role ORDER BY id
    </select>

    <select id="findById" resultType="org.example.entity.SysRole">
        SELECT * FROM sys_role WHERE id = #{id}
    </select>

    <select id="findByUserId" resultType="org.example.entity.SysRole">
        SELECT r.* FROM sys_role r
        INNER JOIN sys_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_role (role_name, role_code, remark) VALUES (#{roleName}, #{roleCode}, #{remark})
    </insert>

    <update id="update">
        UPDATE sys_role SET role_name=#{roleName}, role_code=#{roleCode}, remark=#{remark} WHERE id=#{id}
    </update>

    <delete id="deleteById">DELETE FROM sys_role WHERE id = #{id}</delete>

</mapper>
