<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.mapper.WorkRecordMapper">

    <select id="list" resultType="org.example.entity.WorkRecord">
        SELECT * FROM work_record
        <where>
            <if test="name != null and name != ''">AND name LIKE CONCAT('%', #{name}, '%')</if>
            <if test="companyName != null and companyName != ''">AND company_name LIKE CONCAT('%', #{companyName}, '%')</if>
            <if test="projectName != null and projectName != ''">AND project_name LIKE CONCAT('%', #{projectName}, '%')</if>
        </where>
        ORDER BY created_at DESC
    </select>

    <insert id="insertBatch" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO work_record (company_name, name, project_name, order_no, actual_start_date, actual_end_date,
            actual_days, standard_days, attendance_days, work_content, source_file, org_id, org_name, attendance_verified)
        VALUES
        <foreach collection="list" item="r" separator=",">
            (#{r.companyName}, #{r.name}, #{r.projectName}, #{r.orderNo}, #{r.actualStartDate}, #{r.actualEndDate},
             #{r.actualDays}, #{r.standardDays}, #{r.attendanceDays}, #{r.workContent}, #{r.sourceFile},
             #{r.orgId}, #{r.orgName}, #{r.attendanceVerified})
        </foreach>
    </insert>

    <delete id="deleteById">
        DELETE FROM work_record WHERE id = #{id}
    </delete>

    <select id="listByName" resultType="org.example.entity.WorkRecord">
        SELECT * FROM work_record WHERE name = #{name}
    </select>

    <select id="findById" resultType="org.example.entity.WorkRecord">
        SELECT * FROM work_record WHERE id = #{id}
    </select>

    <update id="updateAttendanceVerified">
        UPDATE work_record SET attendance_verified = #{val}, attendance_days = #{attendanceDays} WHERE id = #{id}
    </update>

</mapper>
